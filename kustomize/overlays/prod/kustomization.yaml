apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: Namespace
    patch: |-
      - op: add
        path: /metadata/labels/environment
        value: prod
